COMMAND message text
SYNTAX message text <text-expression>
TOKENS KEYWORD(message) KEYWORD(text) EXPRESSION
AST MessageText(expression, folded_text)
SEMANTIC expression must resolve to text, reject unknown variables and type mismatches
CODEGEN write folded UTF-16LE message into MessageBox PE template
TEST_VALID message text "Hello, " + name
TEST_INVALID message text "Active: " + active
LIMITATIONS compile-time folding only, no runtime string engine
