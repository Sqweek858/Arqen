# BlendMixToCode Semantic Changes

Rules:

- code must be int
- only literal `0` supported initially
- bool/text values are rejected
- int variables can wait for a later milestone

Possible errors:

```text
expected int code
unsupported exit code
```
