# BlendMixToCode Codegen Changes

Initial codegen:

```text
blend mix to code 0 -> ExitProcess(0)
```

This should be equivalent to current:

```text
exit 0
```

No new imports are needed for code 0.
